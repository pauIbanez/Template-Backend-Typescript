// The type for the data you want as the user's registration data
interface RegistrationData {
  firstName: string;
  lastName: string;
  picture: string;
  phoneNumber: string;
  password: string;
}

export default RegistrationData;
